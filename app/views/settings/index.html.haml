- title "Inställningar"
%section.box.index
  %h1.box-title= h1
  %table.full.wrap
    %thead
      %tr
        %th Namn
        %th Värde

    %tbody
      - @settings.each do |setting|
        %tr
          %td= link_to setting.human_name, edit_setting_path(setting)
          %td= setting.value
