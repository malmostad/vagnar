- title "Bokningsarkiv"
%section.box.index
  %h1.box-title= h1
  %table.full.wrap
    %thead
      %tr
        %th Företag
        %th Plats
        %th Tid

    %tbody
      - @bookings.each do |booking|
        %tr
          %td= booking.company.name
          %td= booking.place.name
          %td= date_and_time(booking)
