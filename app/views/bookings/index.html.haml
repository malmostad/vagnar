- title "Aktuella bokningar"
%section.box.index
  %h1.box-title= h1
  .box-content
    .help-block
      = "Nuvarande period varar #{serving_period_interval(@booking_period)}."
      = "Det går att boka tider #{booking_period_interval(@booking_period)}."
  %table.full.wrap
    %thead
      %tr
        %th Aktör
        %th Plats
        %th Tid
        %th

    %tbody
      - @bookings.each do |booking|
        %tr
          %td= booking.company.name
          %td= booking.place.name
          %td= date_and_time(booking)
          -# TODO: dummy data
          - rand = rand(1..2)
          - if rand == 1
            %td
              .actions= link_to 'Boka', bookings_path, class: "btn btn-primary btn-sm"
          - else
            %td.delete= button_to 'Avboka', booking, method: :delete, data: { confirm: 'Är du säker?' }, class: "btn btn-danger btn-sm"
